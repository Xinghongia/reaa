# 第二章：数据层 - 学习笔记与问答

## 本章学习内容

### 2.1 为什么需要数据库？

```
┌─────────────────────────────────────────────────────────────┐
│                    没有数据库的应用                           │
├─────────────────────────────────────────────────────────────┤
│  用户打开阅读器                                            │
│       ↓                                                   │
│  APP 获取书籍列表                                          │
│       ↓                                                   │
│  请求网络获取... ❌ 慢！离线不能用！                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    有数据库的应用                            │
├─────────────────────────────────────────────────────────────┤
│  用户打开阅读器                                            │
│       ↓                                                   │
│  APP 从本地数据库获取 → 立刻显示（快）                      │
│       ↓                                                   │
│  同时后台同步网络数据                                       │
│       ↓                                                   │
│  数据库更新 → UI 自动刷新                                  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Room 三要素

```
┌─────────────────────────────────────────────────────────────┐
│                    Room 数据库三要素                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐               │
│  │ Entity  │    │   DAO   │    │ Database│               │
│  │ 实体类  │    │ 数据操作 │    │ 数据库   │               │
│  └────┬────┘    └────┬────┘    └────┬────┘               │
│       │               │              │                    │
│   定义"表结构"     定义"操作"      持有连接                 │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  Entity    →  表格的列定义（bookUrl, name, author...）      │
│  DAO       →  SQL 语句封装（insert, delete, query...）      │
│  Database  →  数据库本身，持有所有 DAO                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 Entity（实体类）详解

#### Book Entity 结构图

```
┌─────────────────────────────────────────────────────────────┐
│                      Book 实体类                             │
├─────────────────────────────────────────────────────────────┤
│  @Entity(tableName = "books")                              │
│  data class Book(                                          │
│      @PrimaryKey  var bookUrl: String = ""  ──→ 主键（唯一） │
│      var name: String = ""              ──→ 书名             │
│      var author: String = ""             ──→ 作者           │
│      var coverUrl: String? = null       ──→ 封面URL        │
│      var intro: String = ""              ──→ 简介           │
│      var charset: String = "UTF-8"       ──→ 编码          │
│      var lastChapterTime: Long = 0      ──→ 最后阅读时间   │
│      var totalChapterNum: Int = 0      ──→ 章节数         │
│      ...                                                    │
│  )                                                         │
└─────────────────────────────────────────────────────────────┘
```

#### 为什么用 bookUrl 作为主键？

```
┌─────────────────────────────────────────────────────────────┐
│              为什么用 bookUrl 作为主键？                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 每本书的 URL 是唯一的                                   │
│  2. 不需要额外的自增 ID                                     │
│  3. 直接用 URL 查询更快                                     │
│                                                             │
│  对比两种设计：                                             │
│                                                             │
│  方案A（自增ID）：                                          │
│  ┌────────────────────────────────────────┐                 │
│  │ id │ bookUrl        │ name            │                 │
│  │ 1  │ https://xxx/1  │ 斗破苍穹         │                 │
│  │ 2  │ https://xxx/2  │ 凡人修仙传       │                 │
│  └────────────────────────────────────────┘                 │
│  查询书籍需要：WHERE id = 1                                 │
│                                                             │
│  方案B（URL作主键）：                                       │
│  ┌────────────────────────────────────────┐                 │
│  │ bookUrl           │ name              │                 │
│  │ https://xxx/1     │ 斗破苍穹           │                 │
│  │ https://xxx/2     │ 凡人修仙传         │                 │
│  └────────────────────────────────────────┘                 │
│  查询书籍需要：WHERE bookUrl = 'https://xxx/1'              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.4 DAO（数据访问对象）详解

#### DAO 方法与 SQL 对应

```
┌─────────────────────────────────────────────────────────────┐
│                    DAO 方法与 SQL 对应                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  @Dao                                                       │
│  interface BookDao {                                        │
│                                                             │
│      @Insert  →  INSERT INTO books ...                     │
│      suspend fun insert(book: Book)                         │
│                                                             │
│      @Delete  →  DELETE FROM books WHERE ...                │
│      suspend fun delete(book: Book)                        │
│                                                             │
│      @Query("SELECT * FROM books ORDER BY ...")             │
│      fun getAll(): Flow<List<Book>>   ← 返回 Flow           │
│                                                             │
│      @Query("SELECT * FROM books WHERE bookUrl = :url")    │
│      suspend fun getByBookUrl(url: String): Book?          │
│                                                             │
│  }                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Flow 响应式原理

```
┌─────────────────────────────────────────────────────────────┐
│                  Flow 响应式数据流                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  数据库更新                                                  │
│      ↓                                                      │
│  DAO 发射新数据（Flow）                                     │
│      ↓                                                      │
│  Repository 传递                                            │
│      ↓                                                      │
│  ViewModel 转换为 StateFlow                                  │
│      ↓                                                      │
│  Compose UI 自动重组（recomposition）                        │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  Flow 特点：                                                │
│  • 异步发射数据                                             │
│  • 数据变化时自动通知订阅者                                  │
│  • 可以取消订阅                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.5 Database（数据库）详解

#### 单例模式

```
┌─────────────────────────────────────────────────────────────┐
│                    Database 单例模式                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  @Volatile  ─→ 保证线程间立即可见                           │
│  private var INSTANCE: AppDatabase? = null                 │
│                                                             │
│  fun getInstance(): AppDatabase {                          │
│      return INSTANCE ?: synchronized(this) {               │
│          INSTANCE ?: buildDatabase().also {                │
│              INSTANCE = it                                 │
│          }                                                  │
│      }                                                      │
│  }                                                          │
│                                                             │
│  ─────────────────────────────────────────────────────     │
│                                                             │
│  为什么用单例？                                              │
│  • 数据库连接是重量级对象                                   │
│  • 多次创建浪费资源                                         │
│  • 全局只需要一个实例                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.6 Repository（仓库模式）

#### 为什么需要 Repository？

```
┌─────────────────────────────────────────────────────────────┐
│              不用 Repository 的问题                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ViewModel                                                 │
│      ↓                                                      │
│   DAO ──→ 数据库                                            │
│    ↓                                                        │
│   网络数据源？ ──→ 代码混乱！                               │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│              用 Repository 的好处                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ViewModel                                                 │
│      ↓                                                      │
│   Repository ──→ 统一的数据访问接口                         │
│      ↓                    ↓                    ↓            │
│   DAO ◄─────────── 网络数据源 ◄── 文件数据源                │
│                                                             │
│   优点：                                                    │
│   • 屏蔽数据来源差异                                         │
│   • 统一的数据访问逻辑                                       │
│   • 便于测试和替换实现                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.7 完整数据流向

```
┌─────────────────────────────────────────────────────────────┐
│                    完整数据流向图                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                      UI 层 (Compose)                   │  │
│  │   BookShelfScreen ← observes ← BookViewModel          │  │
│  └────────────────────────────┬───────────────────────────┘  │
│                               │ collects StateFlow           │
│                               ↓                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                     ViewModel 层                       │  │
│  │   BookShelfViewModel ← calls ← BookRepository          │  │
│  └────────────────────────────┬───────────────────────────┘  │
│                               │ calls                        │
│                               ↓                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                   Repository 层                        │  │
│  │   BookRepository ← uses ← BookDao                      │  │
│  └────────────────────────────┬───────────────────────────┘  │
│                               │ SQL                          │
│                               ↓                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                      DAO 层 (Room)                    │  │
│  │   BookDao ← Room 编译时生成实现类                      │  │
│  └────────────────────────────┬───────────────────────────┘  │
│                               │ SQLite                       │
│                               ↓                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │                 Database (SQLite)                      │  │
│  │   AppDatabase ← books.db 文件                          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

***

## 问答记录

## Q1: 什么是依赖？跟继承有什么区别？

**Q**: 什么是依赖？这跟 Java 中的继承父类有什么区别吗？

**A**:

### 定义对比

| 概念     | 回答的问题     | 关系         |
| ------ | --------- | ---------- |
| **继承** | "A 是什么？"  | A **是** B  |
| **依赖** | "A 需要什么？" | A **使用** B |

### 继承示例

```kotlin
// 继承关系：RssSource "是" BookSource 的一种
open class BookSource { ... }
class RssSource : BookSource() { ... }
```

形成纵向的层级关系：

```
        BookSource（父类）
            ↑
            │
      ┌─────┴─────┐
      │           │
  BookSource   RssSource（子类）
```

### 依赖示例

```kotlin
// 依赖关系：BookRepository "使用" BookDao
class BookRepository(
    private val bookDao: BookDao  // 依赖作为参数传入
) {
    fun getBooks(): Flow<List<Book>> {
        return bookDao.getAll()  // 使用 BookDao 的方法
    }
}
```

形成横向的协作关系：

```
    BookRepository  ──使用──→  BookDao
         │                        ↑
         │                        │
    （我不继承它）              （被注入进来）
```

### 为什么优先用依赖？

**测试场景**：想测试 `BookRepository`，但不想用真实数据库

```
┌─────────────────────────────────────────────────────────────┐
│              继承方式（不好）                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  open class BookRepository {                               │
│      open fun getBooks() = dao.getAll()                    │
│  }                                                         │
│                                                             │
│  class FakeRepository : BookRepository() {                  │
│      override fun getBooks() = fakeData  // 必须重写方法    │
│  }                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              依赖方式（好）                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  class BookRepository(private val bookDao: BookDao)         │
│                                                             │
│  // 测试时直接传入假数据                                     │
│  val fakeDao = FakeBookDao()                               │
│  val repo = BookRepository(fakeDao)  // 传入假的就行        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Legado 项目中的例子

```
┌─────────────────────────────────────────────────────────────┐
│                    Legado 中的实例                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  继承：                                                     │
│  sealed class BookSource { ... }  ← 不同类型的书源"是"书源  │
│  class HttpSource : BookSource()                           │
│  class LocalSource : BookSource()                          │
│                                                             │
│  依赖：                                                     │
│  class BookRepository(                                      │
│      private val bookDao: BookDao,      ← 依赖 DAO          │
│      private val bookSourceDao: BookSourceDao               │
│  )                                                          │
│                                                             │
│  class ReadViewModel(                                       │
│      private val bookRepository: BookRepository  ← 依赖 Repo│
│  ) : ViewModel()                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 总结

| <br />   | 继承              | 依赖              |
| -------- | --------------- | --------------- |
| **关系**   | "是...的一种"       | "使用"            |
| **代码表现** | `class A : B()` | `class A(b: B)` |
| **耦合度**  | 高               | 低               |
| **灵活性**  | 低               | 高               |

***

## Q2: Koin 的 get() 函数是如何工作的？

**Q**: Koin 的 `get()` 函数是如何工作的？查看代码中 `viewModel { BookViewModel(get()) }`

**A**:

### Koin 的 get() 原理

`get()` 是 Koin 的核心函数，从容器中解析并获取依赖。

### 代码示例

```kotlin
val appModule = module {
    // get() 自动从容器中获取 BookRepository
    viewModel { BookViewModel(get()) }
}

// 容器定义顺序
val appModule = module {
    single { BookRepository(get()) }  // 先定义这个
    viewModel { BookViewModel(get()) }  // get() 找到上面的 BookRepository
}
```

### 工作原理

```
┌─────────────────────────────────────────────────────────────┐
│                    get() 工作流程                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 调用 get<BookRepository>()                            │
│         ↓                                                   │
│  2. Koin 在模块中查找 BookRepository 的定义                 │
│         ↓                                                   │
│  3. 如果是 single { BookRepository(...) }，返回已创建的实例 │
│         ↓                                                   │
│  4. 如果没有，创建一个新实例并缓存（如果是 single）          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 带参数的构造函数

```kotlin
class BookRepository(private val bookDao: BookDao) { ... }

val appModule = module {
    single { BookDao() }  // 提供 BookDao
    single { BookRepository(get()) }  // get() 自动获取 BookDao
}
```

**关键点**：`get()` 会**自动递归解析**所有依赖。

### 懒加载

```kotlin
// 立即获取
val repository: BookRepository = get()

// 懒加载（推荐用于 ViewModel）
val repository: BookRepository by inject()  // 第一次使用时才解析
```

### 总结

`get()` 的工作流程：

1. 从 Koin 容器中查找对应类型
2. 自动解析该类型的所有依赖（递归调用 get()）
3. 返回实例（single 作用域返回同一个实例）

***

## Q3: 什么叫 Flow 响应式数据流？

**Q**: 什么叫 Flow 响应式数据流？

**A**:

### 什么是"响应式"？

```
┌─────────────────────────────────────────────────────────────┐
│                    现实中的响应式例子                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📺 电视遥控器                                                │
│  ├── 你按"音量+" → 电视音量增加 → 你看到变化                │
│  └── 你不按 → 电视不变 → 不耗精力去"检查"音量               │
│                                                             │
│  ❌ 非响应式（旧式风扇）                                      │
│  ├── 你要不断伸手去摸风扇叶片检查转速                        │
│  └── 你不知道转速变化，除非伸手去摸                           │
│                                                             │
│  ✅ 响应式（现代风扇）                                        │
│  ├── 你设定转速 → 风扇自动保持                               │
│  └── 转速变化时，风扇自动调整                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 传统方式 vs 响应式

```
┌─────────────────────────────────────────────────────────────┐
│                    传统方式：轮询（Pull）                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  用户打开书架                                                │
│       ↓                                                    │
│  APP：给我书籍列表！（请求）                                │
│       ↓                                                    │
│  数据库：这是当前的书籍列表（响应）                          │
│       ↓                                                    │
│  显示书籍列表                                                │
│       ↓                                                    │
│  用户等待...                                                │
│       ↓                                                    │
│  数据库变了，但 APP 不知道！                                 │
│       ↓                                                    │
│  用户必须手动刷新！                                          │
│                                                             │
│  问题：每次想知道新数据，都要主动"问"一遍                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    响应式：发布-订阅（Push）                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐         ┌──────────┐         ┌──────────┐  │
│  │  数据库   │ ──新数据──→ │  APP     │ ──自动──→ │   UI    │  │
│  │ (发布者)  │         │ (订阅者)  │         │  刷新    │  │
│  └──────────┘         └──────────┘         └──────────┘  │
│       ↑                                                    │
│       │                                                    │
│  数据库变了，                                               │
│  不用你问，                                                 │
│  自动告诉你！                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Kotlin Flow 是什么？

```
┌─────────────────────────────────────────────────────────────┐
│                      Flow 是什么？                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Flow = 数据的"河流"                                       │
│                                                             │
│  • 生产者（Producer）：产生数据                               │
│  • 消费者（Consumer）：接收数据                               │
│  • 中间操作（Operators）：加工数据                           │
│                                                             │
│  ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐   │
│  │ 数据库 │──→ │ Flow   │──→ │ map    │──→ │ collect│   │
│  │        │    │        │    │ 加工    │    │        │   │
│  └────────┘    └────────┘    └────────┘    └────────┘   │
│                 数据流               数据流         数据流    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Flow 在 Legado 中的使用

```kotlin
// DAO 层：发布数据
@Dao
interface BookDao {
    @Query("SELECT * FROM books")
    fun getAll(): Flow<List<Book>>  // ← 发布书籍列表
}

// Repository 层：传递数据
class BookRepository(private val bookDao: BookDao) {
    fun getAllBooks(): Flow<List<Book>> = bookDao.getAll()
}

// ViewModel 层：转换数据
class BookShelfViewModel(
    private val bookRepository: BookRepository
) : ViewModel() {
    val books: StateFlow<List<Book>> = bookRepository.getAllBooks()
        .stateIn(viewModelScope, SharingStarted.Lazily, emptyList())
}

// UI 层：消费数据
@Composable
fun BookShelfScreen(viewModel: BookShelfViewModel = viewModel()) {
    val books by viewModel.books.collectAsState()
    //            ↑
    //    订阅 books，当数据变化时自动重组 UI
}
```

### 完整数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                      Flow 完整数据流                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐                                              │
│  │   数据库     │  ← 书籍数据变化（插入/删除/修改）               │
│  └──────┬──────┘                                              │
│         ↓ 发射新数据                                           │
│  ┌─────────────┐                                              │
│  │ Flow<List> │  ← BookDao.getAll() 返回的 Flow               │
│  └──────┬──────┘                                              │
│         ↓ 传递                                                 │
│  ┌─────────────┐                                              │
│  │ Repository  │  ← BookRepository.getAllBooks()              │
│  └──────┬──────┘                                              │
│         ↓ 转换为 StateFlow                                     │
│  ┌─────────────┐                                              │
│  │ StateFlow   │  ← ViewModel 中的 books                      │
│  └──────┬──────┘                                              │
│         ↓ collectAsState()                                     │
│  ┌─────────────┐                                              │
│  │ Compose UI  │  ← recomposition 自动发生                     │
│  └─────────────┘                                              │
│                                                                 │
│  ────────────────────────────────────────────────────────────  │
│                                                                 │
│  用户行为流程：                                                │
│  1. 用户打开书架 → 显示当前书籍                                │
│  2. 后台下载了新章节 → 数据库更新                              │
│  3. Flow 检测到变化 → 自动推送新数据                           │
│  4. Compose 收到新数据 → 自动刷新 UI                           │
│  5. 用户看到最新内容 → 无需手动刷新！                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 对比：Flow vs 普通 List

| <br />   | 普通 List   | Flow         |
| -------- | --------- | ------------ |
| **返回时机** | 一次性返回当前数据 | 数据变化时自动推送新数据 |
| **更新通知** | ❌ 需要手动刷新  | ✅ 自动通知       |
| **线程**   | 同步返回      | 可以在不同线程      |
| **取消**   | 无法取消      | 可以取消订阅       |

### 总结

```
┌─────────────────────────────────────────────────────────────┐
│                        总结                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Flow = 响应式数据流                                         │
│                                                             │
│  • 你订阅（collect）一个数据源                               │
│  • 数据源变化时，自动推送新数据给你                          │
│  • 你收到数据后，自动更新 UI                                │
│                                                             │
│  好处：                                                    │
│  • 不需要手动刷新                                           │
│  • 数据实时同步                                             │
│  • 代码更简洁                                               │
│                                                             │
│  在 Legado 中的流程：                                       │
│  数据库变化 → Flow 发射 → Repository 传递 → ViewModel 转换  │
│       → Compose 订阅 → UI 自动刷新                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

***

## Q4: Repository 感觉就是 Service 业务层，DAO 就是 Mapper 层啊

**Q**: Repository 感觉就是 Service 业务层，DAO 就是 Mapper 层啊。跟 Java 不是一样的嘛？

**A**:

### 架构对比

```
┌─────────────────────────────────────────────────────────────┐
│              Java 传统三层架构 vs Kotlin 简化架构           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 Java 传统三层                          │   │
│  │                                                     │   │
│  │   Controller  ──→  Service  ──→  Mapper  ──→ DB    │   │
│  │   (控制层)        (业务层)      (映射层)             │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 Legado 简化架构                       │   │
│  │                                                     │   │
│  │   ViewModel  ──→  Repository  ──→  DAO  ──→ DB     │   │
│  │   (视图模型)      (仓库层)       (数据访问)           │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  你说的对：Repository ≈ Service，DAO ≈ Mapper              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 为什么 Legado 省略 Service 层？

```
┌─────────────────────────────────────────────────────────────┐
│              Legado 为什么省略 Service 层？                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Java 项目：业务逻辑复杂，需要 Service 层封装          │   │
│  │                                                     │   │
│  │  class BookService {                               │   │
│  │      fun buyBook() { ... }  // 验证、事务            │   │
│  │      fun processOrder() { ... } // 复杂流程          │   │
│  │  }                                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Legado：数据访问为主，业务逻辑相对简单               │   │
│  │                                                     │   │
│  │  Repository 主要做：                                 │   │
│  │  • 数据转换（Entity ↔ Model）                        │   │
│  │  • 多数据源组合（数据库 + 网络）                     │   │
│  │  • 简单的增删改查                                     │   │
│  │                                                     │   │
│  │  复杂业务在 Model 层（analyzeRule）处理              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Repository vs Service

| <br />    | Repository（仓库）             | Service（服务）                   |
| --------- | -------------------------- | ----------------------------- |
| **设计思想**  | 封装数据访问，做数据的中转站             | 封装业务逻辑，做业务处理                  |
| **典型方法**  | `getBooks()`, `saveBook()` | `buyBook()`, `processOrder()` |
| **事务处理**  | 一般不在这里管                    | 通常在这里开启事务                     |
| **业务复杂度** | 简单                         | 复杂                            |

### DAO vs Mapper

```
┌─────────────────────────────────────────────────────────────┐
│                  DAO vs Mapper 对比                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  DAO（Data Access Object）- Room                            │
│  interface BookDao {                                        │
│      @Insert void insert(Book book);                       │
│      @Query("SELECT * FROM books") List<Book> getAll();     │
│  }                                                         │
│                                                             │
│  Mapper（MyBatis）                                          │
│  interface BookMapper {                                    │
│      @Select("SELECT * FROM books") List<Book> getAll();   │
│      // 或 XML 中写 SQL                                     │
│  }                                                         │
│                                                             │
│  Room DAO ≈ MyBatis Mapper（都是 ORM 映射层）               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 架构对应关系总结

```
┌─────────────────────────────────────────────────────────────┐
│                  架构对应关系                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Legado (Kotlin)           Java (传统)                     │
│  ─────────────────────────────────────────────────────────  │
│  ViewModel  ──→  Repository ──→  DAO ──→  SQLite         │
│       ↑              ↑            ↑                       │
│       │              │            │                       │
│  Controller ──→  Service  ──→  Mapper ──→  MySQL         │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  ViewModel  ≈ Controller（接收指令，协调其他层）             │
│  Repository ≈ Service（对内提供数据访问）                   │
│  DAO        ≈ Mapper（SQL 映射，数据持久化）                │
│                                                             │
│  区别：                                                    │
│  • Java 分层更细，适合复杂业务                              │
│  • Legado 简化后适合数据驱动的阅读器场景                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**结论**：你说得对！Legado 的架构就是 Java 三层架构的简化版。

***

## Q5: BookRepository.kt 是什么功能？

**Q**: BookRepository.kt 这个文件是什么功能？

**A**:

### 文件位置与结构

```
app/src/main/java/io/legado/app/data/repository/BookRepository.kt
                                    ↓
                              repository 包

┌─────────────────────────────────────────────────────────────┐
│                    BookRepository 类                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  class BookRepository(                                      │
│      private val bookDao: BookDao,        ← 依赖 BookDao    │
│      private val bookChapterDao: BookChapterDao             │
│  )                                                          │
│                                                             │
│  方法：                                                     │
│  1. getBookCoverByNameAndAuthor()  → 根据书名+作者查封面   │
│  2. getChapterTitle()               → 获取章节标题         │
│  3. getBook()                       → 根据 URL 获取书籍     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                    依赖注入关系图                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  BookRepository                                            │
│       │                                                     │
│       ├── 依赖 ──→ BookDao (书籍数据访问)                    │
│       │                   │                                │
│       │                   └── 负责：书籍的增删改查          │
│       │                                                     │
│       └── 依赖 ──→ BookChapterDao (章节数据访问)           │
│                               │                            │
│                               └── 负责：章节的增删改查       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### getBookCoverByNameAndAuthor() 流程

```
┌─────────────────────────────────────────────────────────────┐
│           getBookCoverByNameAndAuthor() 流程                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入：bookName, bookAuthor                                 │
│         ↓                                                   │
│  执行：withContext(Dispatchers.IO) 切换到 IO 线程          │
│         ↓                                                   │
│  查询：bookDao.getBook(bookName, bookAuthor)               │
│         ↓                                                   │
│  转换：?.getDisplayCover()  获取显示用的封面 URL            │
│         ↓                                                   │
│  返回：String? (封面 URL 或 null)                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 为什么要切换线程？

```
┌─────────────────────────────────────────────────────────────┐
│                    为什么要切换线程？                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  IO 线程池：适合网络请求、文件读写、数据库操作               │
│                                                             │
│  主线程（UI线程）                                           │
│  ❌ 不能做数据库操作（会卡 UI）                              │
│                         ↓ 切换                              │
│  IO 线程池                                                  │
│  ✅ 数据库查询可以在这个线程执行                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### getChapterTitle() 流程

```
┌─────────────────────────────────────────────────────────────┐
│                 getChapterTitle() 流程                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  输入：bookName, bookAuthor, chapterIndex                   │
│         ↓                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  第一步：根据书名+作者查书籍                         │   │
│  │  book = bookDao.getBook(bookName, bookAuthor)       │   │
│  │  bookUrl = book?.bookUrl                            │   │
│  └─────────────────────────────────────────────────────┘   │
│         ↓                                                   │
│  判断：bookUrl 是否为空？                                   │
│         ├── 空 → 返回 null                                 │
│         └── 非空 → 继续                                     │
│         ↓                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  第二步：根据 bookUrl + 章节索引查章节标题            │   │
│  │  bookChapterDao.getChapterTitleByUrlAndIndex()      │   │
│  └─────────────────────────────────────────────────────┘   │
│         ↓                                                   │
│  返回：String? (章节标题或 null)                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 在分层架构中的位置

```
┌─────────────────────────────────────────────────────────────┐
│                    Legado 分层架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ViewModel                                                  │
│    │ 调用                                                   │
│    ↓                                                        │
│  BookRepository  ← 你现在看的文件                           │
│    │ 依赖                                                   │
│    ↓                                                        │
│  BookDao / BookChapterDao                                  │
│    │ SQL                                                    │
│    ↓                                                        │
│  SQLite (Room)                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 方法对比

| 方法                            | 参数         | 返回值    | 复杂度      |
| ----------------------------- | ---------- | ------ | -------- |
| `getBookCoverByNameAndAuthor` | 书名+作者      | 封面URL  | 简单       |
| `getChapterTitle`             | 书名+作者+章节索引 | 章节标题   | 中等（两步查询） |
| `getBook`                     | bookUrl    | Book对象 | 简单（直接透传） |

### 总结

**BookRepository 的职责**：

1. **封装数据访问** - 提供统一的方法给 ViewModel 调用
2. **线程切换** - 用 `withContext(Dispatchers.IO)` 避免阻塞 UI
3. **数据组装** - 可以组合多个 DAO 的操作（如 `getChapterTitle` 需要先查书再查章节）

## Q6: SearchRepository 接口的实现类在哪？

**Q**: SearchRepository 接口的实现类在哪？

**A**:

### 答案：接口和实现类在同一个文件

```
┌─────────────────────────────────────────────────────────────┐
│                    文件位置                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  data/repository/SearchRepository.kt                       │
│         ├── 第 23 行：interface SearchRepository          │
│         └── 第 54 行：class SearchRepositoryImpl          │
│                     ↑                                       │
│                     同一个文件里！                          │
│                                                             │
│  Koin 注册：di/appModule.kt                                 │
│         └── 第 75 行                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 代码结构

```kotlin
// SearchRepository.kt

interface SearchRepository {  // 第 23 行：接口定义
    val enabledGroups: Flow<List<String>>
    fun searchBookshelf(query: String): Flow<List<BookShelfItem>>
    // ...
}

class SearchRepositoryImpl(  // 第 54 行：实现类
    private val appDb: AppDatabase,
) : SearchRepository {
    // 实现接口的方法
}
```

### Koin 注册

```kotlin
// app/src/main/java/io/legado/app/di/appModule.kt 第 75 行
single<SearchRepository> { SearchRepositoryImpl(get()) }
```

### 依赖注入流程

```
┌─────────────────────────────────────────────────────────────┐
│                    Koin 依赖注入                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  appModule.kt                                               │
│       │                                                    │
│       └── single<SearchRepository> {                      │
│               SearchRepositoryImpl(get())                  │
│           }                                                │
│                    │                                     │
│                    └── get() 解析 AppDatabase            │
│                                                             │
│  使用时：                                                   │
│  class SearchViewModel(                                    │
│      private val repository: SearchRepository  ← 直接用接口 │
│  )                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 两种设计风格对比

```
┌─────────────────────────────────────────────────────────────┐
│              BookRepository vs SearchRepository            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  BookRepository                     SearchRepository        │
│  ─────────────────                  ──────────────────────  │
│  • 只有一个类                      • 有接口 + 实现类        │
│  • 没有接口                        • 同一文件              │
│  • 简单场景，不需要接口            • 复杂场景，需要规范    │
│                                                             │
│  为什么 SearchRepository 用接口？                            │
│  • search 功能复杂，有多种状态                             │
│  • 便于单元测试时替换实现                                  │
│  • 定义清晰的 API 规范                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### SearchSession 事件流

```
┌─────────────────────────────────────────────────────────────┐
│                  SearchSession 事件流                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  SearchSession                                              │
│       │                                                    │
│       ├── events: Flow<SearchSessionEvent>  ← 事件流        │
│       │                                                    │
│       └── 方法：                                           │
│            ├── search()   开始搜索                         │
│            ├── stop()     停止搜索                         │
│            ├── pause()    暂停搜索                         │
│            ├── resume()   恢复搜索                         │
│            └── close()    关闭会话                        │
│                                                             │
│  ──────────────────────────────────────────────────────   │
│                                                             │
│  事件类型：                                                 │
│  • Started        → 搜索开始                               │
│  • Progress       → 搜索进度（包含当前结果）                │
│  • Finished       → 搜索完成                               │
│  • Canceled       → 搜索取消                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 总结

1. **接口和实现类在同一文件**：`SearchRepository.kt`
2. **Koin 注册在**：`appModule.kt` 第 75 行
3. **为什么用接口**：SearchRepository 功能复杂，用接口定义规范，便于测试和替换实现

## Q7: Koin 依赖注入是什么？

**Q**: Koin 依赖注入是啥？Koin 是个给安卓 / Kotlin 用的轻量版 Spring IoC 容器，干的事和 Spring 的 Bean 容器一模一样，连核心思想都没换。启动时把类全交给 bean 容器管理，要用就拿，不需要每次都 new。我这样理解对吗？

**A**:

**完全正确！**

### IoC 核心思想

```
┌─────────────────────────────────────────────────────────────┐
│                    IoC 控制权反转                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  传统方式：                                                 │
│  class A {                                                 │
│      fun doSomething() {                                   │
│          val b = B()  // 需要时自己 new                      │
│          b.doIt()                                          │
│      }                                                     │
│  }                                                         │
│                                                             │
│  IoC 方式：                                                │
│  class A(private val b: B) {  // 不 new，靠外面传入         │
│      fun doSomething() {                                   │
│          b.doIt()  // 直接用                                │
│      }                                                     │
│  }                                                         │
│                                                             │
│  控制权反转：A 的控制权交给外部                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Koin vs Spring Bean 容器

```
┌─────────────────────────────────────────────────────────────┐
│              Koin vs Spring - 核心一样                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Spring                                                    │
│  ─────────────────────────────                             │
│  @Bean                                                    │
│  class Config {                                           │
│      @Bean                                                 │
│      fun bookRepository() = BookRepository()              │
│  }                                                         │
│                                                             │
│  @Autowired                                               │
│  class BookService(Config config) { ... }                  │
│                                                             │
│  ──────────────────────────────────────────────────────   │
│                                                             │
│  Koin                                                      │
│  ─────────────────────────────                             │
│  val appModule = module {                                  │
│      single { BookRepository() }  // 注册到容器              │
│      viewModel { BookViewModel(get()) }  // get() 取      │
│  }                                                         │
│                                                             │
│  共同点：                                                  │
│  • 容器管理 Bean/Singleton                                │
│  • 依赖自动解析（get() / @Autowired）                      │
│  • 生命周期管理                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Koin 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    Koin 工作流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  1. 启动阶段                         │   │
│  │                                                |   │   │
│  │  startKoin {                                   |   │   │
│  │      modules(appModule)                        |   │   │
│  │  }                                             |   │   │
│  │                                                |   │   │
│  │  作用：把所有类注册到容器里                       |   │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  2. 使用阶段                         |   │   │
│  │                                                |   │   │
│  │  viewModel { BookViewModel(get()) }             |   │   │
│  │       |                                          |   │   │
│  │       └── get<BookRepository>()                 |   │   │
│  │               |                                   |   │   │
│  │               └── 容器自动创建/返回                |   │   │
│  │                                                |   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ────────────────────────────────────────────────────────  │
│                                                             │
│  传统方式：                                                │
│  val repo = BookRepository()  // 每次都 new                │
│  val vm = BookViewModel(repo)                              │
│                                                             │
│  Koin 方式：                                                │
│  val vm: BookViewModel by viewModel()  // 不需要 new      │
│           // Koin 自动帮你创建好                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### get() 递归解析过程

```
┌─────────────────────────────────────────────────────────────┐
│                    get() 解析过程                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  viewModel { BookViewModel(get()) }                        │
│                    │                                       │
│                    ↓                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  get<BookRepository>()                              │   │
│  │         │                                           │   │
│  │         ↓                                           │   │
│  │  在容器里找 BookRepository                          │   │
│  │         │                                           │   │
│  │         ↓                                           │   │
│  │  single { BookRepository(...) } → 返回已创建的实例 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  递归解析：                                                │
│                                                             │
│  get<BookRepository>()                                    │
│       │                                                    │
│       ├── 容器里找 → 找到了！返回                         │
│       │                        │                          │
│       │                        └── BookRepository(get()) │
│       │                                     │               │
│       │                                     ↓               │
│       │                          get<BookDao>()            │
│       │                                     │               │
│       │                                     ↓               │
│       │                          容器里找 BookDao           │
│       │                                     │               │
│       │                                     ↓               │
│       │                          single { BookDao() }       │
│       │                                     │               │
│       │                                     ↓               │
│       │                                返回 BookDao         │
│       │                                                    │
│       ↓                                                    │
│  返回 BookRepository（依赖已自动注入）                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 总结

```
┌─────────────────────────────────────────────────────────────┐
│                    你的理解完全正确                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ✅ Koin = 轻量级 Spring IoC 容器                          │
│  ✅ 核心思想：控制权反转（IoC）                              │
│  ✅ 启动时注册，用时获取，不用每次 new                       │
│  ✅ 自动解析依赖关系                                        │
│                                                             │
│  唯一区别：                                                │
│  • Spring 用注解（@Autowired, @Bean）                       │
│  • Koin 用 DSL（module { single { } }）                    │
│                                                             │
│  这是 Kotlin 特有的风格，用函数式代码替代注解，              │
│  更简洁，更灵活。                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Q8: Koin 依赖注入关键字含义和语法

**Q**: appModule.kt 中 Koin 依赖注入的关键字含义和语法是什么意思？

**A**:

### 文件结构概览

```
┌─────────────────────────────────────────────────────────────┐
│                    appModule.kt 结构                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  val appModule = module {                                  │
│                                                             │
│      // 1. DAO 注册（single 单例）                         │
│      single { get<AppDatabase>().readRecordDao }           │
│                                                             │
│      // 2. Repository 注册                                   │
│      singleOf(::ReadRecordRepository)                      │
│      single<SearchRepository> { SearchRepositoryImpl(get()) }│
│                                                             │
│      // 3. ViewModel 注册                                    │
│      viewModelOf(::SearchViewModel)                        │
│      viewModel { (route: ReplaceEditRoute) -> ... }         │
│                                                             │
│  }                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 核心关键字表

```
┌─────────────────────────────────────────────────────────────┐
│                    Koin 核心关键字                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  关键字        │ 作用              │ 返回值                 │
│  ──────────────┼──────────────────┼─────────────────      │
│  module { }    │ 定义一个模块      │ KoinModule            │
│  single { }    │ 注册单例         │ SingleBuilder         │
│  singleOf( )   │ 自动注入单例      │ SingleBuilder         │
│  viewModel { } │ 注册 ViewModel   │ ViewModelBuilder      │
│  viewModelOf() │ 自动注入 ViewModel│ ViewModelBuilder      │
│  get()         │ 从容器获取依赖    │ T                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### single { } vs singleOf()

```
┌─────────────────────────────────────────────────────────────┐
│              single { } vs singleOf()                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  single { BookRepository(get(), get()) }                  │
│  • 手动写构造函数调用                                       │
│  • 显式控制依赖关系                                         │
│  • 适合有特殊创建逻辑的情况                                 │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  singleOf(::BookRepository)                                │
│  • Koin 自动调用构造函数                                   │
│  • 自动解析所有依赖                                         │
│  • 更简洁，但构造函数必须可访问                             │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  :: 是 Kotlin 的函数引用                                   │
│  singleOf(::BookRepository) 等价于：                       │
│  single { BookRepository(get(), get()) }                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### single<T> { } 带接口注册

```
┌─────────────────────────────────────────────────────────────┐
│              single<T> { } 带接口注册                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  single<SearchRepository> { SearchRepositoryImpl(get()) } │
│       │                            │                        │
│       │                            └── 实现类构造           │
│       │                                                │
│       └── 注册类型：SearchRepository 接口                  │
│                                                             │
│  使用时：                                                  │
│  class SearchViewModel(                                    │
│      private val repository: SearchRepository  ← 接口类型   │
│  )                                                         │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  好处：                                                    │
│  • 面向接口编程                                            │
│  • 便于替换实现（如测试时用 FakeRepository）               │
│  • 降低耦合度                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### viewModel { } vs viewModelOf()

```
┌─────────────────────────────────────────────────────────────┐
│              viewModel { } vs viewModelOf()                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  viewModelOf(::SearchViewModel)                            │
│  等价于：                                                  │
│  viewModel { SearchViewModel(get(), get(), get()) }        │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  viewModel { } 带参数：                                    │
│  viewModel { (route: ReplaceEditRoute) ->                  │
│      ReplaceEditViewModel(                                 │
│          app = get(),                                      │
│          replaceRuleDao = get(),                           │
│          route = route                                     │
│      )                                                     │
│  }                                                         │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  single vs viewModel：                                     │
│  single → 全局单例，整个应用只有一个实例                    │
│  viewModel → 按页面创建，每个页面有自己的实例               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### get() 解析过程

```
┌─────────────────────────────────────────────────────────────┐
│                    get() 解析过程                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  single { BookRepository(get(), get()) }                   │
│               │            │                                │
│               │            └── get<BookChapterDao>()        │
│               └── get<BookDao>()                           │
│                                                             │
│  解析顺序：                                                │
│  1. 调用 get<BookRepository>()                            │
│  2. 找到 single 定义，执行表达式                           │
│  3. 遇到 get<BookDao>()，递归解析                          │
│  4. 找到 single 定义，返回 BookDao 实例                    │
│  5. 同样解析 BookChapterDao                               │
│  6. 调用构造函数，返回 BookRepository 实例                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Legado 中的实际注册代码

```kotlin
val appModule = module {

    // 1. DAO - single 单例
    single { get<AppDatabase>().bookDao }
    single { get<AppDatabase>().bookChapterDao }

    // 2. Repository - singleOf 自动注入
    singleOf(::BookRepository)
    singleOf(::ReadRecordRepository)

    // 3. Repository - 带接口注册
    single<SearchRepository> { SearchRepositoryImpl(get()) }
    single<ExploreRepository> { ExploreRepositoryImpl(get()) }

    // 4. ViewModel - viewModelOf 自动注入
    viewModelOf(::SearchViewModel)
    viewModelOf(::BookshelfViewModel)

    // 5. ViewModel - 带参数
    viewModel { (route: ReplaceEditRoute) ->
        ReplaceEditViewModel(
            app = get(),
            replaceRuleDao = get(),
            route = route
        )
    }
}
```

### 总结

```
┌─────────────────────────────────────────────────────────────┐
│                    Koin 关键字总结                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────┬─────────────────────────────────────┐   │
│  │ single { }    │ 手动创建单例                        │   │
│  ├───────────────┼─────────────────────────────────────┤   │
│  │ singleOf(::)  │ 自动注入单例（推荐）                 │   │
│  ├───────────────┼─────────────────────────────────────┤   │
│  │ single<T> { }│ 注册为接口类型                       │   │
│  ├───────────────┼─────────────────────────────────────┤   │
│  │ viewModel { }│ 注册 ViewModel（可传参数）           │   │
│  ├───────────────┼─────────────────────────────────────┤   │
│  │ viewModelOf()│ 自动注入 ViewModel（推荐）           │   │
│  ├───────────────┼─────────────────────────────────────┤   │
│  │ get<T>()     │ 从容器获取依赖                       │   │
│  └───────────────┴─────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

***

## Q9: AppDatabase 和 DatabaseMigrations 数据库配置与迁移

**Q**: AppDatabase.kt 和 DatabaseMigrations.kt 数据库配置与迁移都有些什么内容？每次更新都要修改这个文件吗？最近一次修改是什么？

**A**:

### AppDatabase.kt 结构

```
┌─────────────────────────────────────────────────────────────┐
│                   AppDatabase.kt 结构                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  @Database(                                                │
│      version = 85,                                         │
│      entities = [Book, BookSource, BookChapter, ...],     │
│      views = [BookSourcePart],                             │
│      autoMigrations = [AutoMigration(43→44), ...]         │
│  )                                                        │
│  abstract class AppDatabase : RoomDatabase() {            │
│                                                             │
│      // DAO 抽象属性                                       │
│      abstract val bookDao: BookDao                         │
│      abstract val bookChapterDao: BookChapterDao           │
│      // ... 20+ 个 DAO                                   │
│                                                             │
│      companion object {                                    │
│          val dbCallback = object : Callback() {            │
│              // 数据库首次创建时的初始化                      │
│          }                                                 │
│      }                                                     │
│  }                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### DatabaseMigrations.kt 结构

```
┌─────────────────────────────────────────────────────────────┐
│                DatabaseMigrations.kt 结构                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  object DatabaseMigrations {                                │
│                                                             │
│      val migrations: Array<Migration> = arrayOf(           │
│          migration_10_11,                                   │
│          migration_11_12,                                  │
│          // ... 34 个手动迁移                              │
│          migration_82_83,                                  │
│      )                                                     │
│                                                             │
│      private val migration_10_11 = object : Migration(...)  │
│      private val migration_82_83 = object : Migration(...)  │
│                                                             │
│      // AutoMigration 规格类                                │
│      class Migration_54_55 : AutoMigrationSpec { ... }     │
│      class Migration_64_65 : AutoMigrationSpec { ... }     │
│  }                                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 迁移版本一览

```
┌─────────────────────────────────────────────────────────────┐
│                   数据库迁移版本一览                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  v10→11 │ 删除重建 txtTocRules 表                          │
│  v11→12 │ rssSources 添加 style                           │
│  v12→13 │ rssSources 添加 articleStyle                     │
│  v13→14 │ books 表大改（重建+索引）← 破坏性迁移             │
│  v14→15 │ bookmarks 添加 bookAuthor                         │
│  v15→17 │ 创建 readRecord 表                               │
│  v17→18 │ 创建 httpTTS 表                                  │
│  v18→19 │ readRecord 添加 androidId                        │
│  v19→20 │ book_sources 添加 bookSourceComment              │
│  v20→21 │ book_groups 添加 show                            │
│  v21→22 │ books 表再重建（添加 readConfig）← 破坏性迁移      │
│  v22→23 │ chapters 添加 baseUrl                            │
│  v23→24 │ 创建 caches 表                                   │
│  v24→25 │ 创建 sourceSubs 表                               │
│  v25→26 │ sourceSubs → ruleSubs                            │
│  v26→27 │ bookmarks 表大改（重建）← 破坏性迁移              │
│  v27→28 │ rssArticles/rssStars 添加 variable               │
│  v28→29 │ rssSources 添加 sourceComment                    │
│  v29→30 │ chapters 添加 startFragmentId/endFragmentId     │
│  v30→31 │ readRecord 结构变更（deviceId）                  │
│  v31→32 │ 删除 epubChapters 表                             │
│  v32→33 │ bookmarks 重建+索引                              │
│  v33→34 │ book_groups 添加 cover                           │
│  v34→35 │ book_sources 添加 concurrentRate                 │
│  v35→36 │ book_sources 添加 loginUi/loginCheckJs          │
│  v36→37 │ rssSources 添加 loginUrl/loginUi/loginCheckJs    │
│  v37→38 │ book_sources 添加 respondTime                    │
│  v38→39 │ rssSources 添加 concurrentRate                   │
│  v39→40 │ chapters 添加 isVip/isPay                        │
│  v40→41 │ httpTTS 添加 loginUrl/header/concurrentRate     │
│  v41→42 │ httpTTS 添加 contentType                         │
│  v42→43 │ chapters 添加 isVolume                          │
│  ─────────────────────────────────────────────────────    │
│  v82→83 │ readRecord/readRecordDetail/Session 大改         │
│          │（添加 bookAuthor 字段）← 最近一次修改            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 自动迁移 vs 手动迁移

```
┌─────────────────────────────────────────────────────────────┐
│              自动迁移 (AutoMigration) vs 手动迁移           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────┬─────────────────────────────────┐ │
│  │     AutoMigration   │          Migration              │ │
│  ├─────────────────────┼─────────────────────────────────┤ │
│  │ Room 自动推断变更    │ 手动编写 SQL                    │ │
│  │ 代码简洁             │ 代码复杂                         │ │
│  │ 仅支持简单变更       │ 支持所有变更                     │ │
│  │ 添加字段 ✅          │ 删除字段 ✅                      │ │
│  │ 重命名字段 ✅        │ 重建表 ✅                        │ │
│  │ 删除字段 ❌          │ 复杂数据转换 ✅                  │ │
│  └─────────────────────┴─────────────────────────────────┘ │
│                                                             │
│  使用场景：                                                 │
│  • 新版本添加字段 → AutoMigration（推荐）                  │
│  • 删除/重命名字段 → Migration                              │
│  • 表结构大改 → Migration（通常重建表）                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 最近一次修改 v82→83 详解

```
┌─────────────────────────────────────────────────────────────┐
│               v82→83 迁移：readRecord 表大改                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  原因：同名书不同作者的问题                                  │
│  例：《斗破苍穹》天蚕土豆 vs 同名书其他作者                 │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  变更前：                                                   │
│  ┌──────────────────────────────┐                          │
│  │ readRecord                   │                          │
│  │ PRIMARY KEY(deviceId,        │                          │
│  │              bookName)       │ ← 仅靠书名区分            │
│  └──────────────────────────────┘                          │
│                                                             │
│  变更后：                                                   │
│  ┌──────────────────────────────┐                          │
│  │ readRecord                   │                          │
│  │ PRIMARY KEY(deviceId,        │                          │
│  │              bookName,        │                          │
│  │              bookAuthor)      │ ← 新增 author 区分        │
│  │ bookAuthor TEXT NOT NULL      │                          │
│  │ readTime INTEGER              │                          │
│  │ lastRead INTEGER              │                          │
│  └──────────────────────────────┘                          │
│                                                             │
│  ─────────────────────────────────────────────────────    │
│                                                             │
│  实现方式：重建表 + 数据迁移                                 │
│  1. ALTER TABLE rename to _old                            │
│  2. CREATE TABLE 新结构                                    │
│  3. INSERT ... SELECT 迁移数据                             │
│  4. DROP TABLE _old                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 迁移流程

```
┌─────────────────────────────────────────────────────────────┐
│                    数据库迁移执行流程                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  用户升级 App (v1 → v2)                                     │
│           │                                                │
│           ▼                                                │
│  ┌─────────────────┐                                       │
│  │ 打开现有数据库   │ v1 版本存在                           │
│  └────────┬────────┘                                       │
│           │                                                │
│           ▼                                                │
│  ┌─────────────────┐                                       │
│  │ 查找迁移路径     │ v1 → v2                              │
│  │ Migration?      │ 是否有 Migration(v1, v2)             │
│  └────────┬────────┘                                       │
│           │                                                │
│           ▼                                                │
│      ┌────┴────┐                                          │
│      │ 存在？   │                                          │
│      └────┬────┘                                          │
│         Yes│                                                 │
│           │                                                 │
│           ▼                                                │
│  ┌─────────────────┐                                       │
│  │ 执行 migrate()  │ 执行 SQL 语句                          │
│  └────────┬────────┘                                       │
│           │                                                │
│           ▼                                                │
│  ┌─────────────────┐                                       │
│  │ 更新 version    │ v1 → v2                               │
│  └─────────────────┘                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 什么时候需要修改这两个文件？

```
┌─────────────────────────────────────────────────────────────┐
│                什么时候修改 AppDatabase.kt                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 新增 Entity → 添加到 entities 列表                       │
│  2. 新增视图   → 添加到 views 列表                          │
│  3. 版本升级   → 添加 autoMigrations 或 migrations          │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│              什么时候修改 DatabaseMigrations.kt              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 简单字段变更 → AutoMigrationSpec 即可                   │
│     例：添加字段、删除字段（配合 @DeleteColumn）             │
│                                                             │
│  2. 复杂变更   → 手写 Migration                             │
│     • 重建表并迁移数据                                       │
│     • 跨表数据转换                                          │
│     • 删除表                                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

***

## 相关资源

- [第二章：数据层](../02_data_layer/README.md)
- [第三章：业务逻辑层](../03_model_layer/README.md)

