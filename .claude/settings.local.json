{
  "permissions": {
    "allow": [
      "Bash(./gradlew assembleDebug)",
      "Bash(./gradlew --stop)",
      "Bash(./gradlew clean *)",
      "WebSearch",
      "Bash(./gradlew assembleDebug --no-daemon)"
    ]
  }
}
